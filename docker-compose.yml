version: "3"
services:
  nodejs:
    build: .
    ports:
      - "3322:3322"
    networks:
      - app-network
  db:
    image: mongo:4.2
    container_name: kate_mongo
    volumes:
      - dbdata:/data/db
    networks:
      - app-network
networks:
  app-network:
volumes:
  dbdata:
